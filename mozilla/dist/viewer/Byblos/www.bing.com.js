/* Stele for bing front page   Cameron Kaiser   Public domain */function() {	return {		wantURI : function(uri) {			if (uri.path == "/" || uri.path == "")				return true;			return false;		},		parseHTML : function(text) {			var page;						// Kill style sheet			page = text.replace(/<style type="text\/css".+<title>/, "<title>");						return {				response : "ok",				body     : page			};		}	};};